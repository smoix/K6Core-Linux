# Build outputs (generated by build.sh, multi-GB raw disk images)
disk.img
disk-gui.img
*.img.zip

# macOS
.DS_Store

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Claude Code project instructions (not part of the published project)
CLAUDE.md

# Test media (copyrighted, not part of the published project)
*.mp3
